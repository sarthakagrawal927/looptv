# No env vars required for basic operation.
# The app uses yt-dlp for catalog building (no API key needed)
# and the YouTube IFrame Player API for playback (free, no key needed).
#
# Optional: YouTube Data API key for embeddable status pre-check
# YOUTUBE_API_KEY=

# SaaS Maker widgets (feedback / changelog / testimonials)
NEXT_PUBLIC_SAASMAKER_API_KEY=
